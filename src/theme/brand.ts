/** Brand constants. Centralized so the name lives in exactly one place. */
export const APP_NAME = 'Rebound';
export const APP_TAGLINE = 'Bounce back, day by day.';
/** Short greeting variants for the Today header. */
export const GREETINGS = ['How are you today?', 'How did today go?', 'Check in with yourself.'];
